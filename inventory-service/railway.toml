[build]
builder = "NIXPACKS"
buildCommand = "cd .. && ./gradlew :inventory-service:bootJar -x test"

[deploy]
startCommand = "java -Xms256m -Xmx512m -jar inventory-service/build/libs/inventory-service-2.0.0.jar --spring.profiles.active=railway"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
