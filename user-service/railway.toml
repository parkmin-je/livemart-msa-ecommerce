[build]
builder = "NIXPACKS"
buildCommand = "cd .. && ./gradlew :user-service:bootJar -x test"

[deploy]
startCommand = "java -Xms256m -Xmx512m -jar user-service/build/libs/user-service-2.0.0.jar --spring.profiles.active=railway"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
